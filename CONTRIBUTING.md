@'
# Contributing Guidelines

Thank you for contributing to the B2B Smart Ordering System – Admin Dashboard.

## Branching Strategy

Do not push directly to `main`.

Create a new branch for each change:

```bash
git checkout main
git pull
git checkout -b feature/feature-name